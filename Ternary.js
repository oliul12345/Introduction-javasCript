// const age = 27;

// age <= 21 ? console.log('Tumi akono soto manus') : console.log('Tomar boyos hoisa');


let price = 2900;
const isLeader = true;

// price = isLeader== true ? price = price > 1000 ? price = price / 2 : 0 : price = price + 100;
price = isLeader== true ? 
price = price > 1000 ?
 price = price / 2 
 : 0 :
  price = price + 100;

// price > 1000 ? price = price / 2 : 0

// if(isLeader == true){
//     if(price > 1000){
//         price = 1000 / 2;
//     }
//     else{
//         price = 0;
//     }
// }
// else{
//     price = price + 100;
// }
console.log(price);